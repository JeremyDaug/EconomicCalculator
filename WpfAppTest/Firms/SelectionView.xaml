<Window x:Class="EditorInterface.Firms.SelectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EditorInterface.Firms"
        mc:Ignorable="d"
        Title="JobSelectionView" Height="150" Width="200">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="Which do you want?" 
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Grid.Row="0" Grid.Column="0"/>
        <ComboBox Grid.Row="1" Grid.Column="0"
                  Name="SelectionOptions"
                  Height="30" Width="150"/>

        <Button Grid.Row="2" Height="30" Width="100"
                Content="Select" 
                ClickMode="Release"
                Click="Button_Click"/>

    </Grid>
</Window>
