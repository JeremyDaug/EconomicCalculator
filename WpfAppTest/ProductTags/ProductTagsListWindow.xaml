<Window x:Class="EditorInterface.ProductTags.ProductTagsListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EditorInterface.ProductTags"
        xmlns:converters="clr-namespace:EditorInterface.Converters"
        mc:Ignorable="d"
        Style="{StaticResource SharedMenu}"
        Title="ProductTagsList" Height="450" Width="800">
    <Window.Resources>
        <converters:EnumListToStringConverter x:Key="converter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <DataGrid Name="ProductTagGrid" AutoGenerateColumns="False"
                  AlternatingRowBackground="LightGray" AlternationCount="2"
                  IsReadOnly="True" MouseDoubleClick="EditTag">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Tag}" Width="100"/>
                <DataGridTextColumn Header="Parameters" 
                                    Binding="{Binding Params, Converter={StaticResource converter}}"
                                    Width="150" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Content="New Tag" HorizontalAlignment="Center" 
                    VerticalAlignment="Center" Width="75" 
                    Grid.Row="0" Grid.Column="0" ClickMode="Release"
                    Click="NewProductTag"/>
            <Button Content="Edit Tag" HorizontalAlignment="Center" 
                    VerticalAlignment="Center" Width="75" 
                    Grid.Row="0" Grid.Column="1" ClickMode="Release"
                    Click="EditTag"/>
            <Button Content="Copy Tag" HorizontalAlignment="Center" 
                    VerticalAlignment="Center" Width="75" 
                    Grid.Row="0" Grid.Column="2" ClickMode="Release"
                    Click="CopyTag"/>
            <Button Content="Save Tags" HorizontalAlignment="Center" 
                    VerticalAlignment="Center" Width="75" 
                    Grid.Row="0" Grid.Column="3" ClickMode="Release"
                    Click="SaveToFile"/>
        </Grid>
    </Grid>
</Window>
