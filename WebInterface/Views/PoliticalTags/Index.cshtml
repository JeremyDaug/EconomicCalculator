@model IEnumerable<EconModels.PopulationModel.PoliticalTag>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Group.Name)
        </th>
		<th>
			@Html.DisplayNameFor(model => model.Tag)
		</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Group.Name)
        </td>
		<td>
			@Html.DisplayFor(modelItem => item.Tag)
		</td>
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.GroupId, tag = item.Tag }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.GroupId, tag = item.Tag })
        </td>
    </tr>
}

</table>
